<!-- provenance: written by stardust:audit phase 0 / master hands-off activation; input: final validation run, redesign stardust.style hands-off; date: 2026-07-02 -->
# Direction log — stardust.style redesign (fable5-e2e final validation)

## Hands-off mode activated (2026-07-02)
Hands-off mode active for the whole pipeline: audit → direct (Mode A brand-faithful,
single canonical direction) → prototype → migrate → deploy → diff. All interactive
gates auto-resolve per master SKILL.md § Hands-off mode; quality gates run at full
strength. Hard blockers only: unreachable source, unrecoverable DA_TOKEN.

---

# Active direction (resolved 2026-07-02, hands-off)

**Phrase:** "make it dramatically better from a design and SEO/LLM-readability
standpoint while unmistakably stardust.style"

**Restatement (dimensional):** Hold brand identity fixed (palette, type,
grounds, signature moves — Mode A pins) while moving *execution quality*
(craft, scale discipline), *conversion focus*, and a stardust-specific axis
this brand names itself: *machine-readability* (retrieval-ready headings,
static schema, crawlable facts). Register, audience, tone: inherited.

## Movements
- expressive: restrained → committed (within Mode A — the accent is spent, not multiplied)
- distinctiveness: familiar-AI-grammar → distinctive (the card-trio scaffold is replaced by brand-native affordances)
- craft/execution: ad-hoc → disciplined (modular scale, 3-radius vocabulary, pairing rules)
- machine-readability: client-side/implied → static/stated (F-002, F-006, F-013)
- density: **balanced (hands-off default — 7-section home fired the multi-audience floor; captured max section-pad is already 64px, so the floor is also brand-faithful)**
- ia-fidelity: **reimagined (default — audit fixes require structural additions: cta-band, fact panel, variant strip, topic h2s)**
- audience: inherited + AI answer engines promoted to first-class (the site's own positioning)

## Mode detection
signal-strong (8 clustered palette roles, named type families) → **Mode A
brand-faithful, default**. No rebrand trigger in phrase. Mode A+ not
activated: no improvements item names the body face or a palette role as the
defect (the system-stack choice and the amber are the brand; the defects are
scale sprawl, pairing ambiguity, and under-spend — all fixable inside the pins).

## Divergence (brand-faithful mode)
```
decade           reasoned    → 2025-now (all three researched anchors current)
craft            ✓ rolled    → Folded-paper ephemera (md5 byte1=139) → receipt strip
register         ✓ rolled    → Pharmacy insert (md5 byte2=46) → fact panel
ground-family    inherited   → dark (rolled pale-gray overridden: brand-faithful)
font deck        inherited   → brand system stacks (SF Pro Display/Text, SF Mono)
palette          inherited   → existing 8-role ink/dust/amber set
```
Reference research (refero MCP, live): Langbase, Eraser, Metaview — full
styles retrieved; implications recorded in DESIGN.json
extensions.divergence.references_used[]. User-provided anchors: none.

## Improvements lists (Phase 2.5, audit-fed)
- stardust/prototypes/index-improvements.md — 6 items (F-005/F-021, F-008, F-011/F-022, F-013/F-002/F-006, F-004, F-014)
- stardust/prototypes/docs-improvements.md — 5 items (F-003, F-005, F-009/F-017, F-012/F-018/F-006, F-010)

## Named assumptions (hands-off)
1. density: balanced — multi-audience floor fired; captured 64px max makes this brand-faithful too.
2. ia-fidelity: reimagined — required by the audit fixes; IA *priorities* still preserved via extensions.iaPriorities[].
3. Single canonical direction (no variant fan-out) per hands-off default; motion register live-systems selected in DESIGN.json (prototype applies it; static fallback complete).
4. Second prototype page: /docs/ (the site's second template and its conversion page).
5. Mode A+ declined (no evidence-gated type/palette weakness in the improvements lists).

## Command sequence (proposed → executing)
1. $stardust direct (this) → 2. $stardust prototype index (shape → craft →
critique/audit/adapt/motion/vision gates) → 3. $stardust prototype docs →
4. hands-off approve → 5. $stardust migrate → 6. $stardust deploy →
7. $stardust diff index --profile eds

## Confirmation
Hands-off mode: plan recorded here in lieu of interactive confirmation
(master SKILL.md § Hands-off mode). Quality gates unchanged.
